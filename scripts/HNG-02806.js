//HNG group task

const fullName = 'Patrick Timothy Njoli';
const id = 'HNG-02806';
const language = 'JavaScript';
const email = 'patimjoli@gmail.com';

const output = (fullName, id, language, email) => {
  const message= `Hello World! This is ${fullName} with HNGi7 ID ${id} and email ${email} using ${language} for stage 2 task.`;

  console.log(message);
};
output(fullName, id, language, email); 
