'use strict';
const introduceMyself = (fullName, id, preferredLang) => {
  console.log(
    `Hello World, this is ${fullName} with HNGi7 ID ${id} using ${preferredLang} for stage 2 task.`
  );
};

introduceMyself('Ayomide', '04256', 'javascript');
